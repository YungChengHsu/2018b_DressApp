<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorFacebook</item>
        <item name="colorPrimaryDark">@color/menu_button_orange</item>
        <item name="colorAccent">@color/colorFacebook</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="android:actionBarStyle">@style/MyActionBar</item>

        <item name="android:dialogPreferenceStyle">@style/DialogText</item>
    </style>

    <style name="MyActionBar" parent="Theme.AppCompat.NoActionBar">

        <item name="android:actionMenuTextColor">@color/white</item>

    </style>

    <style name="DialogText" parent="@android:style/TextAppearance.Holo.DialogWindowTitle">
        <item name="android:textColor">@color/colorFacebook</item>
    </style>

    <style name="MenuFilterBar" parent="Theme.AppCompat.NoActionBar">

        <item name="android:actionMenuTextColor">@color/menu_button_orange</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Transparent" parent="AppTheme">
        <item name="windowActionBarOverlay">true</item>
    </style>
</resources>
